{
  "name": "luminous-ai-backend",
  "version": "1.0.0",
  "description": "Backend proxy for Luminous AI — keeps the Gemini API key secret and handles chat requests.",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
